[project]
name = "skills-connector-mcp"
version = "0.1.0"
description = "An MCP server that connects skills to an agent."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.28.0",
    "pydantic>=2.0",
    "pyyaml>=6.0.3",
]

[tool.uv]
package = false
